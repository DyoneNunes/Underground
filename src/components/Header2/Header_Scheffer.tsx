import * as S from './HeaderStyles';
import Container from '../Container/Container';

const Header = () => {
    return (
        <S.StyledHeader>
            <Container>
                <S.H2>
                    Scheffer
                </S.H2>
            </Container>
        </S.StyledHeader>
    );
};

export default Header;
