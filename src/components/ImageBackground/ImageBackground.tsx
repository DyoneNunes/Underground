import * as S from './BackgroundStyles';
import Header from "../Header/Header";

function Background() {
    return (
        <S.LandingPage>
            <Header />
        </S.LandingPage>
    );
}

export default Background;
